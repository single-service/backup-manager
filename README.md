# 🗄️ Backup Manager

**Backup Manager** — это мощное open-source решение для автоматизации резервного копирования и восстановления баз данных с поддержкой различных облачных хранилищ и протоколов передачи данных.

[![Docker Hub](https://img.shields.io/badge/Docker%20Hub-singleservice%2Fbackup--manager-blue)](https://hub.docker.com/r/singleservice/backup-manager)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

---

## 📚 Описание

Backup Manager предоставляет удобный веб-интерфейс на базе Django Admin для управления резервными копиями баз данных с возможностью:

- ✅ **Подключение различных СУБД** — поддержка PostgreSQL, MySQL, ClickHouse
- ✅ **Гибкое хранение** — S3, FTP, SFTP, Яндекс.Диск
- ✅ **Автоматизация** — настройка расписания создания бэкапов (ежедневно, еженедельно, ежемесячно)
- ✅ **Ротация бэкапов** — автоматическое удаление старых копий
- ✅ **Простое восстановление** — восстановление БД из дампа в один клик
- ✅ **Проверка подключений** — тестирование доступа к БД и хранилищам
- ✅ **Современный UI** — красивый интерфейс на базе Django Unfold

---

## 🗃️ Поддерживаемые базы данных

| База данных | Поддержка | Утилиты |
|------------|-----------|---------|
| **PostgreSQL** | ✅ | `pg_dump` / `psql` |
| **MySQL/MariaDB** | ✅ | `mysqldump` / `mysql` |
| **ClickHouse** | ✅ | `clickhouse-backup` |

---

## ☁️ Поддерживаемые хранилища

| Хранилище | Тип | Особенности |
|-----------|-----|-------------|
| **S3-совместимые** | S3 | AWS S3, MinIO, DigitalOcean Spaces и др. |
| **FTP** | FTP | Классический FTP с поддержкой пользовательских портов |
| **SFTP** | SFTP | Безопасная передача по SSH |
| **Яндекс.Диск** | Cloud | OAuth авторизация |

### Параметры подключения к хранилищам

#### S3
- **Host**: endpoint URL (например: `https://s3.amazonaws.com`)
- **Bucket Name**: имя bucket'а
- **Access Key**: AWS Access Key ID
- **Secret Key**: AWS Secret Access Key

#### FTP/SFTP
- **Host**: адрес сервера (можно указать порт через `:`, например: `ftp.example.com:2121`)
- **Bucket Name**: базовый путь на сервере (например: `/`, `/backups`, `/p1`)
- **Access Key**: username
- **Secret Key**: password

#### Яндекс.Диск
- **Secret Key**: OAuth токен
- Остальные поля не требуются

---

## 🚀 Быстрый старт

### Docker Compose (рекомендуется)

1. Создайте `docker-compose.yml`:

```yaml
version: '3.8'

services:
  backup-manager:
    image: singleservice/backup-manager:latest
    ports:
      - "8009:8000"
    environment:
      - SITE_HOST=localhost
      - LANGUAGE=ru
      - ADMIN_USERNAME=admin@admin.com
      - ADMIN_PASSWORD=admin
      - DEBUG=1
    volumes:
      - ./database:/app/database
      - ./static:/app/static
    restart: unless-stopped
```

2. Запустите:

```bash
docker-compose up -d
```

3. Откройте браузер: http://localhost:8009/admin

4. Войдите с учетными данными из переменных окружения

---

## ⚙️ Переменные окружения

| Переменная | Описание | Пример значения | Обязательная |
|-----------|----------|-----------------|--------------|
| `SITE_HOST` | Хост сайта для CSRF | `mysite.com` | Нет |
| `LANGUAGE` | Язык интерфейса | `en` или `ru` | Нет (по умолчанию: `en`) |
| `ADMIN_USERNAME` | Email администратора | `admin@admin.com` | Да |
| `ADMIN_PASSWORD` | Пароль администратора | `secure_password` | Да |
| `DEBUG` | Режим отладки Django | `0` или `1` | Нет (по умолчанию: `0`) |

---

## 📖 Использование

### 1. Добавление хранилища

1. Перейдите в раздел **File storages**
2. Нажмите **Add File storage**
3. Заполните поля:
   - **Name**: понятное имя хранилища
   - **Type**: выберите тип (S3, FTP, SFTP, Yandex Disk)
   - Заполните соответствующие поля подключения
4. Сохраните и нажмите **Check connection** для проверки

### 2. Добавление базы данных

1. Перейдите в раздел **Databases**
2. Нажмите **Add Database**
3. Укажите:
   - **Name**: имя базы данных
   - **Type**: тип СУБД (PostgreSQL, MySQL, ClickHouse)
   - **Connection string**: строка подключения

**Примеры строк подключения:**
```
# PostgreSQL
postgresql://user:password@localhost:5432/dbname

# MySQL
mysql://user:password@localhost:3306/dbname

# ClickHouse
clickhouse://user:password@localhost:9000/dbname
```

4. Сохраните и проверьте подключение

### 3. Создание задачи бэкапа

1. Перейдите в раздел **Dump Tasks**
2. Нажмите **Add Dump task**
3. Выберите:
   - **Database**: база данных для бэкапа
   - **File storage**: хранилище для сохранения
   - **Task period**: частота создания бэкапов
   - **Max dumpfiles keep**: количество хранимых копий
4. Сохраните

### 4. Запуск бэкапа

**Ручной запуск:**
1. Выберите задачу в списке
2. Нажмите **Execute dump**

**Автоматический запуск:**
Настройте cron для запуска команды проверки задач:
```bash
# Каждые 10 минут проверять задачи на выполнение
*/10 * * * * cd /path/to/app && python manage.py check_dump_operations
```

### 5. Восстановление из бэкапа

1. Перейдите в раздел **Dump Task Operations**
2. Найдите нужную операцию с успешным статусом
3. Выберите её и нажмите **Restore dump**
4. Дождитесь завершения восстановления

---

## 🏗️ Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                    Django Admin (UI)                        │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      Backup Service                         │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  Database Interface (PostgreSQL/MySQL/ClickHouse)    │   │
│  └──────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  Storage Factory (S3/FTP/SFTP/Yandex.Disk)          │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        ▼                     ▼                     ▼
  ┌──────────┐         ┌───────────┐         ┌──────────┐
  │ Database │         │  Storage  │         │  /tmp/   │
  │ (Source) │         │ (Remote)  │         │ (Local)  │
  └──────────┘         └───────────┘         └──────────┘
```

### Workflow создания бэкапа:

1. **Создание дампа** → `pg_dump`/`mysqldump`/`clickhouse-backup` → `/tmp/dump_file`
2. **Загрузка** → Storage Service → Облачное хранилище
3. **Очистка** → Удаление старых дампов (если превышен лимит)
4. **Очистка локально** → Удаление `/tmp/dump_file`

### Workflow восстановления:

1. **Скачивание** → Storage Service → `/tmp/dump_file`
2. **Восстановление** → `psql`/`mysql`/`clickhouse-backup` → База данных
3. **Очистка** → Удаление `/tmp/dump_file`

---

## 🛠️ Разработка

### Локальный запуск

1. Клонируйте репозиторий:
```bash
git clone https://github.com/your-repo/backup-manager.git
cd backup-manager
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Примените миграции:
```bash
cd apps
python manage.py makemigrations
python manage.py migrate
```

5. Создайте суперпользователя:
```bash
python manage.py init_admin
```

6. Запустите сервер:
```bash
python manage.py runserver
```

### Структура проекта

```
backup_manager/
├── apps/
│   ├── config/              # Настройки Django
│   ├── manager/             # Основное приложение
│   │   ├── admin.py         # Django Admin конфигурация
│   │   ├── models.py        # Модели БД
│   │   ├── services/        # Бизнес-логика
│   │   │   ├── backup_service.py
│   │   │   ├── storage_service.py
│   │   │   ├── storage_factory.py
│   │   │   └── databases/   # Интерфейсы для БД
│   │   └── management/      # Django команды
│   └── manage.py
├── venv/                    # Тестовые скрипты
├── docker-compose.yml
├── Dockerfile
├── requirements.txt
└── README.md
```

---

## 🧪 Тестирование

### Проверка подключения к SFTP
```bash
python venv/test_connection.py
```

### Просмотр файлов на SFTP
```bash
# Простой список
python venv/sftp_files_list.py

# Детальный список конкретной директории
python venv/sftp_files_list.py /path/to/dir

# Интерактивный режим
python venv/sftp_files_list.py -i
```

---

## 🔒 Безопасность

- 🔐 Используйте **надежные пароли** для доступа к админ-панели
- 🔐 Настройте **SSL/TLS сертификаты** для production
- 🔐 Ограничьте доступ к админ-панели через **firewall** или **VPN**
- 🔐 Регулярно обновляйте зависимости для устранения уязвимостей
- 🔐 Храните **SECRET_KEY** в переменных окружения, не в коде
- 🔐 Используйте **read-only** доступ для резервного копирования где возможно

---

## 🐛 Troubleshooting

### Ошибка подключения к базе данных
- Проверьте строку подключения
- Убедитесь, что БД доступна с хоста, где запущен Backup Manager
- Проверьте firewall правила

### Ошибка загрузки в хранилище
- Проверьте правильность учетных данных
- Для S3: убедитесь, что bucket существует
- Для FTP/SFTP: проверьте права доступа к директориям
- Проверьте сетевое подключение

### Дампы не создаются автоматически
- Убедитесь, что настроен cron для запуска `check_dump_operations`
- Проверьте логи Django на наличие ошибок
- Убедитесь, что установлены все необходимые утилиты для дампа (pg_dump, mysqldump, etc.)

---

## 📄 Лицензия

Этот проект распространяется под лицензией **MIT License**.

---

## 🤝 Вклад в проект

Мы приветствуем вклад от сообщества!

- 🐛 Нашли баг? Создайте [Issue](https://github.com/your-repo/backup-manager/issues)
- 💡 Есть идея? Создайте [Pull Request](https://github.com/your-repo/backup-manager/pulls)
- ⭐ Нравится проект? Поставьте звезду на GitHub!

---

## 📞 Поддержка

- 📧 Email: support@example.com
- 💬 Telegram: @backup_manager_support
- 🐦 Twitter: @backup_manager

---

## 📊 Roadmap

- [ ] Поддержка MongoDB
- [ ] Поддержка Redis
- [ ] Webhooks для уведомлений
- [ ] Telegram бот для управления
- [ ] REST API
- [ ] Шифрование дампов
- [ ] Инкрементальные бэкапы
- [ ] Multi-tenancy

---

Сделано с ❤️ для упрощения жизни DevOps инженеров
